---
"shadow-garden": patch
---

The workspace Preview/Code pill no longer flies in from the previous page's position when navigating between components — its shared-layout id is now scoped per slug. Mask Blur also gained its React Bits attribution.
